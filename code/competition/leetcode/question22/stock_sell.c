#include <stdio.h>
void main_algorithm () {
    int number_values = 0;
    printf ("Enter the amount of day to day values you have:");
    scanf ("%d", &number_values);
    int day_to_day [number_values];
    for (int x = 0; x < number_values; x++) {
        printf ("Enter the current day value:");
        scanf ("%d", &day_to_day [x]);
    } int backtrack = 0, fronttrack = 1, total = 0;
    while (1) {

    } printf ("Highest Profit Margin: %d\n", total);
} int main () {
    main_algorithm ();
    return 0;
}
